#pragma once

#include <iostream>
#include <string>
#include <vector>

#include "charsmap.h"

std::vector<std::string> createVector(std::string input);

std::string returnRow(int row, char character);
